# frozen_string_literal: true

source "https://rubygems.org"

# Specify your gem's dependencies in ethotrace-mcp.gemspec
gemspec

# 単体開発(cd gems/ethotrace-mcp)では未公開の core をモノレポ内パスで解決する。
gem "ethotrace", path: "../ethotrace"

gem "irb"
gem "rake", "~> 13.0"

gem "rspec", "~> 3.0"

# 自己観測(dogfooding)を mcp 単独の root から回すための RSpec ライフサイクル
# アダプタ。開発時のみ(script/dogfood)。
gem "ethotrace-rspec", path: "../ethotrace-rspec"

gem "rubocop", "~> 1.21"
